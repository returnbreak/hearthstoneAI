# 炉石卡牌数据抓取总结

本文记录当前炉石传说卡牌数据抓取方案。目标是从 2019 年以后开始，按资料片周期抓取“版本前期、版本中期、版本后期”的卡牌快照，并尽量避开重复 build。

## 当前结论

主数据源使用 HearthstoneJSON：

```text
https://hearthstonejson.com/
https://api.hearthstonejson.com/v1/
```

HearthstoneJSON 可以稳定提供多语言卡牌 JSON。历史快照脚本使用 `zhCN/cards.collectible.json`，并按资料片周期下载标准候选卡牌和狂野候选卡牌。当前卡牌脚本会同时下载 `zhCN/cards.collectible.json` 和 `zhCN/cards.json`：前者用于标准/狂野构筑候选池，后者用于完整 `card_index`，覆盖 token、衍生物、幸运币等非收藏卡。

旅法师营地没有找到稳定公开的标准/狂野构筑卡牌 API。它适合作为人工参考站点，不建议作为自动化主数据源。

套牌数据方面，HearthstoneJSON 不提供套牌列表。当前只加入了公开研究数据集 manifest，不默认下载大体积 CSV。

## 抓取脚本

当前脚本：

```text
tools/fetch_hearthstonejson_expansion_snapshots.ps1
```

当前最新版本抓取脚本：

```text
tools/fetch_latest_hearthstone_cards.py
```

运行方式：

```powershell
.\tools\fetch_hearthstonejson_expansion_snapshots.ps1 -IncludeDeckManifest
```

可选下载套牌数据：

```powershell
.\tools\fetch_hearthstonejson_expansion_snapshots.ps1 -IncludeDeckManifest -DownloadDeckDataset
```

注意：`-DownloadDeckDataset` 会尝试下载 Zenodo 上的 `decks.csv` 和 `cards.csv`，合计约 620MB。

抓取最新标准/狂野卡牌数据与中文 256x 卡牌渲染图：

```powershell
.venv\Scripts\python.exe tools\fetch_latest_hearthstone_cards.py --download-images --image-scope all --workers 32 --timeout 45
```

如果只想更新 JSON，不下载图片：

```powershell
.venv\Scripts\python.exe tools\fetch_latest_hearthstone_cards.py
```

## 下次更新 cards 数据

如果只是更新当前运行时使用的最新卡牌库，优先执行 `tools/fetch_latest_hearthstone_cards.py`。这个脚本会从 HearthstoneJSON 的 `latest` 地址下载中文构筑卡牌数据，并输出给后端直接读取的索引文件。

下载地址写在脚本顶部：

```python
CARD_COLLECTIBLE_JSON_URL = "https://api.hearthstonejson.com/v1/latest/zhCN/cards.collectible.json"
CARD_FULL_JSON_URL = "https://api.hearthstonejson.com/v1/latest/zhCN/cards.json"
IMAGE_URL_TEMPLATE = "https://art.hearthstonejson.com/v1/render/latest/zhCN/256x/{card_id}.png"
```

完整更新 JSON 和 256x 中文卡牌图：

```powershell
.venv\Scripts\python.exe tools\fetch_latest_hearthstone_cards.py --download-images --image-scope all --workers 32 --timeout 45
```

只更新 JSON，不重新下载图片：

```powershell
.venv\Scripts\python.exe tools\fetch_latest_hearthstone_cards.py
```

如果图片已经下载过，脚本会跳过已有且非空的图片文件。重新跑完整命令时，通常只会补新卡或之前失败的图片，不会重复下载所有图片。

更新后重点检查这些文件：

```text
hearthstone_data/cards/cards.collectible.zhCN.json
hearthstone_data/cards/cards.zhCN.json
hearthstone_data/cards/standard.zhCN.json
hearthstone_data/cards/wild.zhCN.json
hearthstone_data/cards/card_index.zhCN.json
hearthstone_data/cards/metadata.json
hearthstone_data/cards/image_manifest.zhCN.json
hearthstone_data/cards/images/render_256x/
```

其中后端运行时真正读取的是：

```text
hearthstone_data/cards/card_index.zhCN.json
```

`card_index.zhCN.json` 是按 `card_id` 建立的字典，方便后端收到 HDT 插件消息后快速按卡牌 ID 补充卡牌名、描述、机制、职业、稀有度和图片路径。

注意：`standard.zhCN.json` 和 `wild.zhCN.json` 仍然来自 `cards.collectible.zhCN.json`，只表示构筑候选池；`card_index.zhCN.json` 来自完整 `cards.zhCN.json`，用于运行时补全公开卡牌、token、衍生物和特殊牌。

更新完成后建议执行一次快速校验：

```powershell
.venv\Scripts\python.exe -c "from ai_backend.cards import CardCatalog; c=CardCatalog.from_latest_data(); e=c.enrich_envelope({'type':'game_event','event':{'type':'card_played','card_id':'EDR_810t'}}); print(e['event'].get('name')); print(e['event'].get('cost')); print(e['event'].get('text')); print(e['event'].get('image',{}).get('render_256x_path'))"
```

预期结果是能打印出 token/衍生物示例卡牌的中文名称、费用、中文描述和 `images/render_256x/<CARD_ID>.png` 图片路径。如果卡牌 ID 已退环境或 HearthstoneJSON 改动，可以把 `EDR_810t` 换成 `card_index.zhCN.json` 中存在的任意卡牌 ID。

还需要检查当前标准环境集合。脚本中的标准集合写在：

```python
DEFAULT_STANDARD_SETS = {
    "CORE",
    "EVENT",
    "EMERALD_DREAM",
    "THE_LOST_CITY",
    "TIME_TRAVEL",
    "CATACLYSM",
}
```

如果炉石新资料片上线或标准轮换发生变化，需要先修改这个集合，再重新运行抓取脚本。否则 `standard.zhCN.json` 会继续按旧标准环境过滤，狂野数据不受这个集合影响。

当前狂野候选排除集合写在：

```python
EXCLUDED_WILD_SETS = {"HERO_SKINS", "PLACEHOLDER_202204"}
```

如果后续 HearthstoneJSON 新增了皮肤、占位、测试卡等非构筑集合，也需要加入排除集合。

## 输出目录

```text
hearthstone_data/
  cards/
    cards.collectible.zhCN.json
    cards.zhCN.json
    standard.zhCN.json
    wild.zhCN.json
    card_index.zhCN.json
    metadata.json
    image_manifest.zhCN.json
    images/
      render_256x/
        <CARD_ID>.png
  decks/
    strategy_context.zhCN.json
  summary.zhCN.json
  selected_snapshots.zhCN.json
  build_metadata.zhCN.json
  skipped_builds.zhCN.json
  decks_manifest/
    manifest.json
  <expansion_key>/
    early_<build>/
      standard.zhCN.json
      wild.zhCN.json
      sets.zhCN.json
      metadata.json
    middle_<build>/
      standard.zhCN.json
      wild.zhCN.json
      sets.zhCN.json
      metadata.json
    late_<build>/
      standard.zhCN.json
      wild.zhCN.json
      sets.zhCN.json
      metadata.json
```

`cards/` 目录用于当前运行时卡牌知识库；`decks/` 目录用于当前运行时套牌和环境策略知识库。历史快照目录用于版本研究和回放分析。

当前主目录命名约定：

- `hearthstone_data/cards/`：卡牌 JSON、卡牌索引和卡牌图片。
- `hearthstone_data/decks/`：套牌、环境原型、己方套牌赢法和对局策略上下文。

旧的 `hearthstone_data/latest/` 和 `hearthstone_data/meta/` 不再作为主数据目录使用；后端只保留兼容读取，方便旧本地数据过渡。

本次生成：

```text
22 个资料片周期
66 个卡牌快照
269 个输出文件
```

## 选择规则

### 1. 枚举 build

脚本会合并两类 build：

- HearthstoneJSON 当前目录页列出的 build
- 手工维护的 2019-2023 历史 build 种子

原因是当前 `https://api.hearthstonejson.com/v1/` 目录页只稳定列出较新的 build，但旧 build 仍然可以通过直接 URL 访问。

### 2. 校验可用性

对每个候选 build 请求：

```text
https://api.hearthstonejson.com/v1/<build>/zhCN/cards.collectible.json
```

使用 HTTP HEAD 读取：

```text
Last-Modified
ETag
Content-Length
```

不可访问的 build 会写入：

```text
hearthstone_data/skipped_builds.zhCN.json
```

### 3. 按资料片周期分组

脚本把 build 按 `Last-Modified` 日期归入资料片周期，例如：

```text
Rise of Shadows
Saviors of Uldum
Descent of Dragons
...
Cataclysm
```

这里的“大版本”指资料片/扩展包周期，不是客户端主版本号。

### 4. 按 ETag 去重

同一资料片周期内，如果多个 build 的 `ETag` 相同，说明 `cards.collectible.json` 内容相同，只保留第一个。

这避免把“客户端 build 变了但构筑卡牌没变”的快照重复下载。

### 5. 每个资料片选 3 个快照

每个资料片周期最多选 3 个：

- `early`：该资料片周期内第一个有卡牌数据变化的快照
- `middle`：周期中间变化量最大的快照
- `late`：下一个资料片上线前最后一个有卡牌数据变化的快照

`middle` 的变化量用相邻快照 `Content-Length` 差值近似估算。它不是精确卡牌 diff，但能优先选中迷你包、平衡补丁、较大文本/数据改动。

## 标准与狂野过滤

HearthstoneJSON 不直接提供：

```text
standard: true
wild: true
```

所以脚本导出的是“标准候选”和“狂野候选”。

狂野候选：

```powershell
$cards |
  Where-Object { $_.collectible -eq $true -and $_.set -ne "HERO_SKINS" }
```

标准候选：

```powershell
$wildCards |
  Where-Object { $standardSetList -contains $_.set }
```

`standardSetList` 按每个资料片周期配置，包含当前标准轮换中的资料片集合。对于历史标准环境，这是近似规则；如果要严格复刻官方当时标准环境，还需要继续校正 Core、Basic、Classic、Legacy 和特殊返场卡。

## 实际选中的快照

| 资料片 | early | middle | late |
|---|---:|---:|---:|
| Rise of Shadows | 30103 | 31761 | 32708 |
| Saviors of Uldum | 33402 | 34670 | 37060 |
| Descent of Dragons | 38034 | 44222 | 44582 |
| Ashes of Outland | 45310 | 53261 | 54613 |
| Scholomance Academy | 56359 | 65000 | 65614 |
| Madness at the Darkmoon Faire | 66927 | 71003 | 74257 |
| Forged in the Barrens | 77662 | 88605 | 88998 |
| United in Stormwind | 91040 | 126451 | 126807 |
| Fractured in Alterac Valley | 127581 | 131660 | 133923 |
| Voyage to the Sunken City | 135540 | 142295 | 145077 |
| Murder at Castle Nathria | 147348 | 155409 | 158725 |
| March of the Lich King | 159202 | 162171 | 170126 |
| Festival of Legends | 170824 | 179020 | 181554 |
| TITANS | 183876 | 188645 | 189017 |
| Showdown in the Badlands | 190145 | 192412 | 194648 |
| Whizbang's Workshop | 195635 | 202360 | 203120 |
| Perils in Paradise | 204283 | 208591 | 210009 |
| The Great Dark Beyond | 210640 | 214839 | 216423 |
| Into the Emerald Dream | 217964 | 223184 | 223542 |
| The Lost City of Un'Goro | 225000 | 226928 | 228587 |
| Across the Timeways | 229984 | 236529 | 238003 |
| Cataclysm | 239422 | 241135 | 242566 |

## 套牌数据

HearthstoneJSON 本身不包含套牌列表、卡组代码、天梯胜率或环境统计。

当前加入的公开套牌数据源：

```text
Hearthstone Decks Dataset
https://zenodo.org/records/10198504
DOI: 10.5281/zenodo.10198504
```

已生成 manifest：

```text
hearthstone_data/decks_manifest/manifest.json
```

该数据集包含：

```text
decks.csv 约 56.1 MB
cards.csv 约 564.1 MB
```

它适合做历史卡组研究、卡牌共现、卡组结构分析，不适合作为实时环境强度来源。

如果需要当前天梯环境、胜率、热门卡组，应该另接实时或准实时来源，例如 HSReplay、HSGuru、官方赛事卡组、社区卡组站。但这些来源通常涉及授权、反爬或付费限制，不应默认写入自动抓取脚本。

## 旅法师营地测试结论

测试页面：

```text
https://www.iyingdi.com/tz/tool/general/cards
https://www.iyingdi.com/tz/tool/general/battlegrounds
```

前端 bundle 中能看到接口名：

```text
/pc/card/get-card-list
/pc/card/get-search-config
/pc/card/get-series-list
/pc/card/get-card-info
```

也能看到可能的域名：

```text
https://api.iyingdi.com
https://api2.iyingdi.com
https://tool.iyingdi.com
```

但直接 POST 未取得稳定构筑卡牌 API 响应：

- `api.iyingdi.com` 返回 404
- `api2.iyingdi.com` 返回 404 或“该页面找不到啦”
- `tool.iyingdi.com` 返回 500

因此不建议把旅法师营地未公开接口作为主抓取链路。

## 后续建议

当前数据已经适合用于：

- 炉石卡牌知识库
- RAG 检索
- 卡牌问答
- 卡牌变化对比
- 标准/狂野候选池分析
- 训练数据生成

后续可以继续补：

- 精确卡牌 diff：新增、删除、费用变化、攻击生命变化、文本变化
- 严格标准环境校正：处理 Core/Legacy/返场/特殊卡
- 套牌数据清洗：从 Zenodo CSV 中抽样并转成卡组分析样本
- 数据库存储：JSON 转 SQLite，方便 Web UI 查询

## 最新版本抓取结果

最近一次 cards 抓取结果：

```text
source: https://api.hearthstonejson.com/v1/latest/zhCN/cards.collectible.json
full_source: https://api.hearthstonejson.com/v1/latest/zhCN/cards.json
Last-Modified: Tue, 02 Jun 2026 17:39:12 GMT
ETag: "4bb385b51f94a8eb639689eff7326b61"
all_collectible_count: 7948
full_card_count: 35046
card_index_count: 35046
wild_count: 6580
standard_count: 1017
render_256x_images: 6580
image_total_size: 约 981 MB
```

当前标准集合按 2026 年 Cataclysm 标准环境配置：

```text
CORE
EVENT
EMERALD_DREAM
THE_LOST_CITY
TIME_TRAVEL
CATACLYSM
```

狂野候选排除：

```text
HERO_SKINS
PLACEHOLDER_202204
```

每张卡会补充图片字段：

```json
{
  "image": {
    "render_256x_url": "https://art.hearthstonejson.com/v1/render/latest/zhCN/256x/END_011.png",
    "render_256x_path": "images/render_256x/END_011.png"
  }
}
```

后端运行时会读取：

```text
hearthstone_data/cards/card_index.zhCN.json
```

`card_index.zhCN.json` 当前由完整 `cards.zhCN.json` 生成，因此可以补全构筑牌以外的公开实体，例如：

```text
EDR_810t 饱胀水蛭
DFT_ALEX_COIN1 幸运币
TIME_875t1 弑君者
CATA_139t 柳牙的腿
```

并在 HDT 消息写入日志前，为公开卡牌数据补充：

```text
text
set
card_class
rarity
mechanics
race / races
spell_school
image
```

补充范围包括：

```text
game_event.event
game_state.hand
game_state.known_enemy_cards
game_state.my_board
game_state.enemy_board
game_state.recent_events
```

这意味着后续 `events.jsonl` 和 `game_state.jsonl` 中的公开卡牌会尽量带上卡牌描述和图片路径。隐藏手牌不会被额外推断。

## 参考来源

- HearthstoneJSON: https://hearthstonejson.com/
- HearthstoneJSON API directory: https://api.hearthstonejson.com/v1/
- Hearthstone Decks Dataset: https://zenodo.org/records/10198504
- 旅法师营地: https://www.iyingdi.com/
